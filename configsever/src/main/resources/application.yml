spring:
  application:
    name: configserver
  profiles:
    active: native
  rabbitmq:
    addresses: ${RABBITMQ_URL}
    connection-timeout: 30000
  cloud:
    config:
      server:
        native:
          search-locations: D:\HOC_TAP\MicroService_springboot\Microservices\configsever\src\main\resources\config
server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: "*"